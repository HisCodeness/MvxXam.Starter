<views:MvxWpfView x:Class="MvxXam.Starter.App.Wpf.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MvxXam.Starter.App.Wpf.Views"
             xmlns:views="clr-namespace:MvvmCross.Wpf.Views;assembly=MvvmCross.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Margin="10" Height="20" Content="Reset" Command="{Binding ResetTextCommand}"/>
            <Button Margin="10" Height="20"  Content="Next" Command="{Binding NextCommand}"/>
            <Label Content="{Binding Time}" Margin="10" />
            <TextBox Text="{Binding Text}" Margin="10"/>
        </StackPanel>
            
    </Grid>
</views:MvxWpfView>
